"""
gesture_module.py — Placeholder
=================================
Future gesture/annotation feature.

Ideas for implementation:
  - Use OpenCV or mediapipe to detect hand gestures via webcam
  - Overlay gesture data (pointer position, highlight rectangles) on the shared screen
  - Send gesture events as small JSON messages alongside screen frames

This file is intentionally empty for the 3-day MVP.
"""

# TODO: Implement gesture control
